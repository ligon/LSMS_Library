# Coverage

The country × feature × wave coverage/readiness model behind
[`ll.coverage()`](../guide/coverage.md) and the `make matrix` CLI.

::: lsms_library.coverage_matrix
    options:
      members:
        - coverage
        - build_matrix
        - grade_feature
        - grade_country_level
        - load_blessed
        - save_snapshot
        - default_snapshot_path
