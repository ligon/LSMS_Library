#+title: Contents

* Overview

Togo EHCVM (Enquete Harmonisee sur les Conditions de Vie des
Menages), the harmonized West African household-conditions survey
template.  One wave configured: =2018=.

* Sampling Design

Standard EHCVM convention: =grappe= is the cluster identifier (=v=);
each grappe is visited in exactly one =vague=, so household
identifier is =(grappe, menage)= without a vague level.  See
CLAUDE.md "EHCVM countries" for the cross-country pattern.

* Sampling universe, exclusions and sub-samples

Evidence for what the Togo wave *claims* to represent, kept deliberately
separate from what the data look like.  Nothing here is inferred from any
=.dta=; every claim is a quotation with a citation.  Companion to
"Sampling Design" above (which records the =grappe=/=vague= identifier
convention) and to "Household Presence / MonthsSpent" below (which
records the 6-month residency filters =s01q12= / =s01q13= that operationalise
"de jure" membership at the person level).

Design representation is out of scope here --- GH #603 is deciding how the
library should *encode* this.  This section is evidence only.

** 2018 (EHCVM 2018/19)

*** Declared universe (verbatim)

#+begin_quote
The survey covered all de jure households excluding prisons, hospitals,
military barracks, and school dormitories.
#+end_quote

Source: World Bank Microdata Library catalog entry *4298*
(=TGO_2018_EHCVM_v02_M=; the id recorded in
=2018/Documentation/SOURCE.org= as =#+CATALOG_ID: 4298=), DDI field
=study_desc.study_info.universe=; rendered on the study-description page
under Coverage > Universe.  Language: English (the source text is in
English --- no translation needed; the local questionnaires are in French).

Verified 2026-07-21 by re-fetching
https://microdata.worldbank.org/metadata/export/4298/json --- byte-identical
to the quote above.  The same record gives:

| DDI field                       | value (verbatim)                     |
|---------------------------------+--------------------------------------|
| =study_info.geog_coverage=      | =National coverage=                  |
| =study_info.analysis_unit=      | =- Household  - Individual  - Community= |
| =method...sampling_deviation=   | (absent)                             |
| =method...weight=               | (absent)                             |
| =method.analysis_info.response_rate= | (absent)                        |

*** WARNING: this universe sentence is NADA boilerplate, not a Togolese claim

The sentence quoted above is *World Bank NADA template text*.  It appears
verbatim in 15 waves across the corpus --- all eight WAEMU/EHCVM countries
(Benin, Burkina Faso x2, Cote d'Ivoire, Guinea-Bissau, Mali, Niger, Senegal,
Togo) and also Ethiopia 2018-19 / 2021-22 and Nigeria 2015-16 / 2018-19 /
2023-24, which are not EHCVM at all.  Its presence attests *which metadata
template was used*; it does **not** independently attest that INSEED made
that claim about this survey.  Do not treat it as a statistical office's
statement about Togo.

Corroborating this reading: the Togo Basic Information Document (INSEED /
World Bank, June 2021; catalog download id 52590) contains **no** universe
statement and **no** exclusion clause at all --- greps for =universe=,
=exclud=, =prison=, =barrack=, =nomad=, =homeless=, =institution= over the
full 20-page text return only an unrelated hit ("access to financial
institutions", Section 6).  The exclusion of collective/institutional
dwellings is asserted *only* by the catalog template.

*** WHERE THE STATEMENT LIVES: catalog only, nothing local

There is **no universe or target-population statement in any file this
repository holds** for this wave.  =2018/Documentation/= contains only
=SOURCE.org= (URL + provenance keywords), =LICENSE.org= (access policy and
citation), and five DVC-tracked questionnaire spreadsheets
(=tgo_ehcvm1_qnr_household_excel_vague{1,2}.xls=,
=tgo_ehcvm1_qnr_community_excel_vague{1,2}.xls=,
=EHCVM_UEMOA_MEN_TOGO_V2.xls=).  No report, no BID, no abstract PDF is
shipped locally.  =_/CONTENTS.org= and =2018/_/notes.org= were grepped for
=univers|populati|menage|couvert|echantill|exclu= --- only incidental hits
on food/nonfood item labels and id-structure notes.

Consequence: for Togo the population record is a *moving target held on the
World Bank's servers*, not something the library holds.  If catalog 4298 is
revised, this section becomes stale silently.  (Togo is one of 41 of 111
waves in this position.)

*** Geographic coverage and the Lome Commune sentence (quoted, NOT corrected)

Catalog 4298, Sampling > Sampling Procedure, opening sentence --- identical
text in the Basic Information Document, section "Sample", *PDF page 10*,
first sentence (both verified 2026-07-21):

#+begin_quote
The Togo EHCVM 2018/19 sample covers all regions with urban and rural areas
surveyed in all regions except Lome Commune where all respondents are from
rural areas.
#+end_quote

*Read that twice.*  It is quoted exactly as printed in both sources.  It is
internally implausible --- Lome Commune is the wholly urban capital district
--- and reads like an urban/rural transposition in the source document
(compare Guinea-Bissau's structurally identical sentence, which says the
autonomous sector of Bissau is the one where "all respondents are from
*urban* areas").  It is reproduced here uncorrected on purpose.  **Do not
silently "fix" it**, and do not treat the corrected reading as documented:
what is documented is the sentence above.

Practical import either way: *Lome Commune is a single-milieu stratum*.
Whichever milieu the source meant, one of urban/rural is absent from Lome
Commune by design, so a rural/urban contrast computed within Lome Commune is
not estimable, and pooled urban/rural comparisons carry a design asymmetry
there.

*** Exclusions --- summary

| kind                | what is excluded                                        | evidence                                        |
|---------------------+---------------------------------------------------------+-------------------------------------------------|
| Population/institutional | prisons, hospitals, military barracks, school dormitories | catalog 4298 Universe field --- *NADA boilerplate*; absent from the BID |
| Geographic (region) | none --- "National coverage"; all regions surveyed      | catalog 4298 Geographic Coverage + Sampling Procedure |
| Geographic (milieu) | one milieu absent in Lome Commune (source says rural-only; see caveat above) | catalog 4298 Sampling Procedure; BID p.10       |
| Security carve-out  | none stated                                             | no =sampling_deviation= field in the DDI record |
| Nomadic / homeless / diplomatic | *not mentioned either way*                  | --- (a gap, not a "no")                          |

Note what is *not* in the record: unlike sibling EHCVM documentation
elsewhere (e.g. the French EHCVM instruments quoted under Burkina Faso,
which exclude =les sans domiciles fixes= and =les menages ayant un statut
diplomatique=), nothing in Togo's catalog entry or BID says anything about
homeless, nomadic, or diplomatic households.  That is silence, not a
documented inclusion.

*** Oversamples and sub-populations: NONE FOUND

No booster, no ethnic or programme sub-sample, no refresh/extended split, no
module subsample, and no cross-section-plus-panel combination is documented
for this wave.  The design as stated is a plain stratified two-stage draw.

The one structure that could be mistaken for a sub-sample is the *two-wave
seasonal split*, which is a fielding split of one sample, not two
populations.  BID p.4, "Survey Characteristics" (verified verbatim):

#+begin_quote
The surveys took place in two waves with each wave covering half of the
sample. The first wave was fielded between September 2018 and December 2018,
while the second wave occurred between April 2019 and June 2019. The
two-wave approach was chosen to account for seasonality of consumption.
#+end_quote

The library folds both fielding waves into the single wave directory =2018=
(=t = "2018"=), so the seasonal half-samples are pooled by default.  Anyone
computing consumption levels should know they are averaging a
Sept--Dec 2018 half and an Apr--Jun 2019 half.

Sample-design magnitudes --- *context, not universe* (catalog 4298 Sampling
Procedure; identical in BID p.10): two-stage design; 540 EAs at stage 1;
12 households per EA at stage 2; total 6,171 households (2,270 urban,
3,901 rural); each EA split into two equal halves, one per fielding wave;
wave 1 = 2,931 households (1,034 urban, 1,897 rural), wave 2 = 3,240
households (1,236 urban, 2,004 rural).

*** Universe vs. what the weights represent

*Unresolvable from the record.*  The DDI entry for 4298 carries **no**
=weight=, no =sampling_deviation= and no =response_rate= field, and the BID
says nothing about what the weights are constructed to represent.  So for
Togo the two cannot be compared: only the (boilerplate) universe is on
record.  Do not assume the weights represent the boilerplate universe ---
nothing states it.

*** Programme context (why the sibling countries look identical)

BID, Introduction, p.3, verbatim:

#+begin_quote
The EHCVM 2018/19 is the first edition of a nationally representative
household survey conducted within the Programme d'Harmonisation et de
Modernisation des enquetes sur les Conditions de Vie des Menages (PHMECV
program), a joint program by the World Bank and the WAEMU Commision. The
main aim of the program is to strengthen the capacity of its member
countries (Benin, Burkina Faso, Cote d'Ivoire, Guinee Bissau, Mali, Niger,
Senegal, and Togo) to conduct living conditions surveys that meet
harmonized, regional standards and to make the collected micro-data publicly
accessible.
#+end_quote

(Transcription notes: accents dropped and curly apostrophes replaced for
ASCII; a footnote marker after "PHMECV program" is dropped and the
parenthesis closed --- the PDF prints "(PHMECV program2" with an unclosed
paren.  "Commision" is the source's own spelling.  Wording otherwise
unaltered.)  A
near-identical universe sentence should therefore be expected for the other
seven EHCVM countries --- and *is* found there, which is exactly why it
cannot be read as a Togo-specific claim.

*** Gap: the =_forEthan= extracts have no documented universe

=2018/Data/= additionally contains non-EHCVM extracts
(=Togo_survey2018_*_forEthan.dta= / =.csv=) shipped with no documentation of
their own.  No universe statement is available for those files, and none was
inferred from their contents.  Recorded as a gap.

**** RESOLVED for the =.csv= half, 2026-09-04 (GH #750)

The five =Togo_survey2018_nonfooditems{7days,30days,3months,6months,12months}.csv=
extracts are *gone*, and the gap they represented is closed rather than
merely noted.  They were =dvc add=ed in 2021 and never pushed: their blobs
are absent from =s3://dvcbucket0/LSMS=, from every local workspace, and
from the L1 cache, so =dvc pull= could never have succeeded for anyone and
their undocumented universe could never have been established.  Their
sidecars were deleted; the md5s are preserved in the commit message
(=retire(Togo): drop the 5 unrecoverable nonfooditems*.csv sidecars=).

What replaced them is documented under "nonfood_expenditures (EHCVM
section 9)" below: the *published* EHCVM modules
=2018/Data1/s09b..s09f_me_tgo2018.dta=, which cover the same five recall
windows and whose universe is the wave's own -- i.e. the one this section
documents at length.  So the substantive gap ("no documented universe") is
answered for the non-food half, by replacing the undocumented source, not
by finding documentation for it.

The =.dta= half is UNCHANGED and the gap above still stands for it:
=Togo_survey2018_fooditems_forEthan.dta= and
=Togo_survey2018_hhroster_forEthan.dta= are still tracked, their blobs *are*
on the remote, and they still have no documented universe.  As of
2026-09-04 the only code that names either is =2018/_/food_expenditures.py=
(which is dead by the same test as the old nonfood script: it is not
declared in =data_scheme.yml=, and the generic =%.parquet= rule in
=_/Makefile= cannot fire for it because the =food_items.org= / =malawi.py=
prerequisites are absent) and =2018/_/notes.org= (a research notebook,
using the forbidden =pd.read_stata=).  Retiring that script -- and with it
the last reader of an undocumented extract -- is the obvious next step, but
it was out of scope for GH #750 and is deliberately left alone.

* nonfood_expenditures (EHCVM section 9)

Item-level household non-food expenditure, from
=2018/Data1/s09b..s09f_me_tgo2018.dta=.  Added 2026-09-04 (GH #750), which
also retired the five unrecoverable CSV extracts described above.

*** SOURCE LOCATION (read this first): the section-9 modules live in
=2018/Data1/= — *NOT* in =2018/Data/=.  =2018/Data/= used to hold five
=Togo_survey2018_nonfooditems*.csv= extracts covering the same five recall
windows; those are gone and unrecoverable.  Do not try to resurrect them.
=Data1/= is the published EHCVM distribution and is complete. ***

** What section 9 is (from the INSTRUMENT, not from convention)

EHCVM section 9 is *conventionally* a recall-period split, and the retired
CSVs were split that way -- but convention is not evidence, so this was
checked against the questionnaire.
=2018/Documentation/tgo_ehcvm1_qnr_household_excel_vague1.xls=, sheets
=S9b__Conso_NA= .. =S9f__Conso_NA=, rows 0-1, verbatim:

#+begin_quote
SECTION 9 : DÉPENSES RÉTROSPECTIVES NON ALIMENTAIRES DU MÉNAGE
PARTIE A: DEPENSES DES FETES ET CEREMONIES AU COURS DES 12 DERNIERS MOIS
PARTIE B: DEPENSES NON ALIMENTAIRES DES 7 DERNIERS JOURS
PARTIE C: DEPENSES NON ALIMENTAIRES DES 30 DERNIERS JOURS
PARTIE D: DEPENSES NON ALIMENTAIRES DES 3 DERNIERS MOIS
PARTIE E: DEPENSES NON ALIMENTAIRES DES 6 DERNIERS MOIS
PARTIE F: DEPENSES NON ALIMENTAIRES DES 12 DERNIERS MOIS
#+end_quote

(=vague1= and =vague2= are byte-identical over section 9 -- diffed, 178
rows each.)  So B..F *are* recall-window modules and =s09?q03= is the
amount spent over that window.  Parts A and B..F are different animals; see
"Partie A is excluded" below.

| file | part | recall window  | items | rows   |
|------+------+----------------+-------+--------|
| s09b | B    | last 7 days    |    17 | 17,932 |
| s09c | C    | last 30 days   |    22 | 27,871 |
| s09d | D    | last 3 months  |    18 | 10,999 |
| s09e | E    | last 6 months  |    12 | 32,326 |
| s09f | F    | last 12 months |    53 | 19,317 |

** The window is carried, NOT annualised

=Expenditure= is the FCFA figure the household reported, and =RecallWindow=
names its window verbatim, so the raw number and its period both stay
recoverable.  Annualising is a *transformation*, and REPORTED-values-only is
the rule this country already applies to =crop_production= and =plot_labor=.
One defensible scaling, spelled out so nobody has to guess:

: annual = Expenditure * 365.25 / {'7 days': 7, '30 days': 30,
:            '3 months': 91.3125, '6 months': 182.625,
:            '12 months': 365.25}[RecallWindow]

=RecallWindow= is a COLUMN, not an index level, because EHCVM gives each
sub-module a *disjoint* item-code block (201-217 / 301-322 / 401-418 /
501-512 / 601-653), so =j= already determines the window.  Making it a level
would bake a functional dependency into the key.  The wave script asserts the
dependency (=nunique() == 1= per =j=), so a future release that reused a code
across modules would fail the build rather than mislabel a window.

Sanity, measured: median expenditure rises monotonically with the window --
500 / 900 / 1,200 / 3,000 / 3,500 FCFA.  A 7-day median at 14.3% of the
12-month median is the shape a correct window mapping produces.

** Partie A (=s09a=) is EXCLUDED — and that has a cost, stated

=s09a= is *not* wired into =nonfood_expenditures=.  Three reasons, in order
of weight:

1. *Different grain.*  It is keyed by EVENT (13 fêtes / cérémonies: Fin du
   Ramadan, Tabaski, Noël, Pâques, Nouvel an, Mariage, Baptêmes,
   Communion/Confirmation, Funérailles, ...), not by product/service, with
   five FIXED amount columns per event rather than one amount per item.
2. *Two of those five columns are food* -- =s09aq03= alimentation and
   =s09aq04= boissons.  They do not belong in a table called
   /non-food/ expenditures at all.
3. *Replacement parity.*  The five retired CSVs were the five recall
   windows, i.e. s09b..s09f.  s09a was not in them either, so excluding it
   loses nothing that was previously delivered.

Folding it in would require either summing over the 13 events (destroying
the event dimension) or minting 39 compound =event × category= item labels
(polluting the =j= axis).  Neither is obviously right, and neither should be
decided silently.

*THE COST, because it is real and easy to miss.*  Sheets =S9e=/=S9f= carry a
standing instruction in the item-label column:

#+begin_quote
(NE PAS INCLURE LES PAGNES, VETEMENTS, CHAUSSURES ET BIJOUX ACHETES A
L'OCCASION DES FETES ET CEREMONIES COMME LA TABASKI, LA FIN DE RAMADAN,
NOEL, PAQUES, NOUVEL AN, MARIAGE, FUNERAILLES, BAPTEMES, ETC. QUI ONT ÉTÉ
INSCRITES DANS LES SECTIONS PRÉCÉDENTES)
#+end_quote

So ceremony clothing, footwear and jewellery were deliberately routed to
=s09aq05= and are therefore *absent from 9E/9F by design*.
=nonfood_expenditures= consequently *UNDERCOUNTS clothing, footwear and
jewellery*.  It is not a bug in the wiring; it is what excluding 9A means,
and anyone using these categories must know it.

*Follow-up (not done here).*  A separate =ceremony_expenditures= feature
keyed =(t, i, event)= with the five reported category columns is the honest
shape for =s09a=: it keeps the event, keeps food and non-food apart, and
does not distort the =j= axis.  =s09a= is a complete grid -- 6,171
households × 13 events = 80,223 rows, of which 13,921 have the gate
=s09aq02 == 1= and carry amounts.

** Grain, keys, and what the framework adds

Declared =(t, i, j)= in =data_scheme.yml=; DELIVERED =(i, t, v, j)=, because
=_join_v_from_sample= adds =v= at API time (540 grappes, no nulls).  Do not
declare =v= here -- =nonfood_expenditures= is not in
=lsms_library/data_info.yml='s =index_info=, so the join fires, and emitting
=v= from the wave script would duplicate it.

No =vague= level: each grappe is visited in exactly one vague, so
=(grappe, menage)= identifies a household uniquely (the standard EHCVM
convention recorded under "Sampling Design").  =i= is the composite id
=format_id(grappe) + '0' + format_id(menage, zeropadding=2)=, inlined
verbatim from =togo.i()= exactly as =livestock.py= does.

=(t, i, j)= is unique *in the source* -- the wave script asserts it -- so
nothing is reduced and =LSMS_GRAIN_STRICT=1= produces zero
=GrainCollapseWarning=.

** Idiosyncrasies worth knowing

- *The modules ship PRE-FILTERED to the gate.*  Every row of s09b..s09f as
  distributed has =s09?q02 == 1= ("Oui"); the "Non" rows are not published.
  So the absence of a =(household, item)= pair means /did not buy/, NOT
  /not asked/.  Do not read a missing pair as missing data.  (=s09a= is the
  opposite: a complete 6,171 × 13 grid with both gate values present.)
  The wave script keeps the gate filter anyway, so a future re-release that
  ships the "Non" rows cannot leak them in as zero-value purchases.
- *A typo in the shipped Stata labels.*  =s09aq02='s variable label reads
  "9A.02. 12 derniers *jours*, avez-vous effectué des dépenses pour les
  fêtes?" where the instrument says "Au cours des 12 derniers *mois*".  The
  instrument is right; the =.dta= label is wrong.  This is exactly why the
  window question was settled against the questionnaire and not against the
  variable labels.
- Relatedly, the =s09aq01= value label for event 02 says "Tabaski *2018*"
  where the questionnaire prints "Tabaski *2017*".  Recorded, not corrected.
- *The item labels come from the INSTRUMENT, not the =.dta=.*  Unlike
  =s07bq03b= (whose value labels were lost -- see "food_acquired units"
  above), the s09 code variables *do* carry Stata value labels.  But those
  are truncated at ~110 characters, so the questionnaire sheets are the
  source for =nonfood_items= in =categorical_mapping.org=.
- *Two declared items are never reported*: 627 "Vignette automobile/moto"
  (Vehicle Road Tax) and 649 "Taxes d'habitation ... taxes de voiries"
  (Property and Municipal Taxes).  120 of 122 items are delivered.  They are
  kept in the table because they are on the instrument; their absence is a
  fact about Togolese households, not a wiring defect.
- *12 of the 6,171 sample households report no section-9 purchase at all* in
  any of the five modules, so =nonfood_expenditures= covers 6,159 households
  (99.81%).  There are zero orphans in the other direction.
- *121 rows report a purchase with no amount* (gate = Oui, =s09?q03= null).
  These are kept: "bought it, amount not recorded" is a reported fact and is
  not the same as "did not buy".

** The =#+name:= trap (if you add or rename an org table)

=df_from_orgfile= (=lsms_library/local_tools.py:1636=) locates a table by
*exact string equality* against ~f'#+name: {name}'.lower()~ after
=.strip().lower()=.  Case is free -- this file mixes =#+name:= and =#+NAME:=
and both work -- but *the single space after the colon is mandatory*, and
nothing may follow the table name on that line.  A miss raises =KeyError=
(GH #461) rather than silently returning the file's *first* table, which is
the behaviour that once labelled every crop with a unit.  Note also that a
bare =get_categorical_mapping(tablename=...)= with no value column returns
an *empty dict* and every lookup then silently yields =pd.NA=; prefer
=code_label_map= (GH #372 / #377 / #348).

** Cross-EHCVM: this lifts to all eight countries, and none of them is wired

All eight EHCVM countries ship the same =s09a..s09f= block, 12 (country,
wave) cells in total, and =Togo/2018= is the only one wired:

| country       | waves with s09    | location                          |
|---------------+-------------------+-----------------------------------|
| Benin         | 2018-19           | =Data1/s09?_me_ben2018.dta=       |
| Burkina Faso  | 2018-19, 2021-22  | =Data1/s09?_me_bfa{2018,2021}.dta= |
| CotedIvoire   | 2018-19           | =Data/Menage/s09?_me_CIV2018.dta= (note the nested dir and the UPPERCASE country tag -- a glob written for the others misses it) |
| Guinea-Bissau | 2018-19           | =Data1/s09?_me_gnb2018.dta=       |
| Mali          | 2018-19, 2021-22  | =Data1/s09?_me_mli{2018,2021}.dta= |
| Niger         | 2018-19, 2021-22  | =Data1/s09?_me_ner{2018,2021}.dta= |
| Senegal       | 2018-19, 2021-22  | =Data1/s09?_me_sen{2018,2021}.dta= |
| Togo          | 2018              | =Data1/s09?_me_tgo2018.dta= — WIRED |

The item codes are the harmonized EHCVM scheme, so the =nonfood_items= table
should port directly, as =harmonize_input= / =harmonize_seed_crop= did from
Niger.  *Verify per country before assuming it* -- that is what the
=nonfood_items= coverage assertion in the wave script is for.

* food_acquired units (Benin<->Togo codebook link)

Togo's =s07bq03b= unit variable stores bare numeric codes (100-662): its
=.dta= lost the Stata value labels, so =convert_categoricals=True= still
returns codes.  The labels survive in the *sibling EHCVM countries* whose
=.dta= kept them --- chiefly *Benin*, which uses the identical standardized
coding (=100=Kg=, =101=Litre=, =107=Boite=, =124=Gousse=, ...).

The decode therefore borrows the sibling labels: the shared codebook
=lsms_library/categorical_mapping/ehcvm_units.org= (=#+name:ehcvm_units=,
built by unioning =s07bq03b= value labels across all 8 EHCVM countries ---
see =slurm_logs/ehcvm_unit_codebook_2026-06-07.org=) is applied to =u= via
=mappings: ['ehcvm_units','Code','Preferred Label']= in
=2018/_/data_info.yml=.  Result: code leaks 25 -> 4.

Residual codes (=114, 659, 660, 662=) are *item-specific local units* --
659 banana/plantain, 660 meat, 662 yam/cassava/plantain portions, 114
tomato -- with the size in =s07bq03c= (Petit/Moyen/Grand).  They have no
label in *any* EHCVM source =.dta=, and the household-questionnaire =Unites=
sheet is the generic 1--57 scheme, NOT the data's 100--700 coding (whose
neighbours -- 663=Agoua, 664=Kpogban -- are specific local names).  No
available source decodes them, so they are *accepted as undecodable* and
left as codes (consistent with Malawi's residuals, #383).  They still
contribute valid food_expenditures and per-native-unit =unitvalue= prices;
only the kg aggregate excludes them.

* Household Presence / MonthsSpent

The EHCVM questionnaire (used in the 2018 wave) does NOT ask
"How many months did [NAME] live here in the past 12 months?"
(the continuous MonthsSpent/MonthsAway variable available in Uganda,
Ethiopia, Tanzania, Malawi, and many other LSMS countries).  Instead,
EHCVM uses two binary eligibility filters:

- =s01q12= :: "Has [NAME] lived continuously in this household for at
  least 6 months?"
- =s01q13= :: "Does [NAME] intend to stay at least 6 months?"

These determine membership eligibility but do not give a continuous
duration measure.  Downstream analysts who need a presence filter for
=household_characteristics= should use these binary variables directly
rather than expecting a =MonthsSpent= column in =household_roster()=.

Togo has no pre-EHCVM waves in the library, so no continuous
months-of-presence variable is available for any wave.

* Grain: two cells that leaned on the framework collapse (GH #323)

Togo tripped =_normalize_dataframe_index='s =groupby().first()= in two
structurally different places.  Both are closed in the CONFIG, per
decision D1 of =slurm_logs/DESIGN_grain_collapse_sites_2026-07-13.org=:
*the core does not aggregate*, so the answer to a duplicated declared
index is to stop manufacturing the duplicate --- never to declare a
reducer.  (=aggregation:= in =data_scheme.yml= is dead config; nothing
reads it, and a test pins that it is not honoured.)

** plot_inputs --- REAL loss, 165 reported line-items destroyed

=plot_inputs= is keyed =(t, i, input, crop, u)=.  =harmonize_input=
maps *every* seed label onto the single =input= value ='Seed'=, so
=crop= is the only level left that can tell two seed line-items apart
--- and =harmonize_seed_crop= was NON-INJECTIVE: four labels shared one
='Autre crop'= catch-all, three of which actually occur in Togo's
=s16b= roster (=Plants/boutures de tubercules= 780 rows, =Autres
semences= 591, =Semences d'autres céréales= 370).  162 households
reported two of them at once, and 165 rows vanished.

The identifier was BROKEN, not a level MISSING: 0 of the 162 colliding
groups repeat the same raw =s16bq01= label, i.e. every collision is
between two *distinct reported items* pooled by a lossy crop map.  The
buckets are now split (=Autre céréale= / =Autre tubercule= / =Autre
crop=), which is also what the source means.  Do NOT "fix" this by
adding =Purchased= to the index --- =Purchased= is a measured attribute
of a line-item, not part of its identity.  See the long note above
=harmonize_seed_crop= in =categorical_mapping.org=, the uniqueness
assertion at the end of =2018/_/plot_inputs.py=, and
=tests/test_gh323_benin_togo.py=.  Measured: API 13,565 -> 13,730.

** cluster_features --- an EXTRACTION bug, value-lossless

=cluster_features= is declared =(t, v)= --- 540 grappes --- but its
=df_main= is the EHCVM *household* cover page
=2018/Data1/s00_me_tgo2018.dta=: 6,171 households.  (=df_geo=,
=grappe_gps_tgo2018.dta=, is already 540 rows, so every duplicate came
from =df_main=.)  The wave frame therefore carried 6,171 rows for a
540-row table --- *all* of them on a duplicated =(t, v)= tuple (no
grappe holds fewer than 3 households), 5,631 of them EXCESS --- for the
framework to reduce.

*No rows of value were destroyed* --- measured, not assumed: all 540
grappes carry exactly one distinct Region (=s00q01=) and one distinct
Rural (=s00q04=), so the reduction was value-lossless and (post-PR
#614) correctly silent.  It is still fixed, because "lossless" here was
an accident nobody was checking: =.first()= on a cluster that stopped
being constant returns a silently *wrong* Region --- the Kazakhstan
1996 / CotedIvoire grappe-648 failure mode.

=2018/_/mapping.py= therefore alias-imports the *shipped* reducer as
its =cluster_features= df_edit hook ---

: from lsms_library.transformations import (
:     collapse_to_cluster_grain as cluster_features,
: )

--- exactly as that helper's docstring prescribes.  It projects to
cluster grain in the extraction and raises =GrainConflict= (naming the
offending grappes) if any column ever varies within one.  Wave frame
6,171 -> 540; API unchanged at 540 rows with identical values;
=assert_frame_equal= passes against the pre-fix API frame.

*Do not re-implement it here.*  The first draft of this fix hand-rolled
a 40-line copy, whose =nunique(dropna=False)= guard read =NaN= as a
CONTRADICTION rather than as absence --- against repo doctrine
(=tests/test_gh323_grain_contract.py=).  It does *not* fire on today's
data (the 5 GPS-less grappes are wholly GPS-less, so their
=nunique(dropna=False)= is still 1), so the defect was LATENT: the
first partially-fixed grappe, or one blank Region, and a correct
completion becomes a dead build.  See =.coder/ledger/323-togo.md=
section 7.

* plot_features (GH #167)

Lasting parcel-level characteristics from the EHCVM agriculture module
=s16a= (Togo is an EHCVM sibling of the Mali reference, PR #284).

*** SOURCE LOCATION (read this first): the agriculture module lives in
=2018/Data1/s16a_me_tgo2018.dta= (9557 parcel rows) — *NOT* in
=2018/Data/=, which holds only =_forEthan= extracts.  Future
maintainers will instinctively look in =Data/= and find nothing. ***

Index =(t, i, plot_id)= where =i= is the EHCVM composite =(grappe,
menage)= household id and =plot_id = "{s16aq02}_{s16aq03}"= (field _
parcel sequence numbers), unique within each household.  =t = "2018"=
(single EHCVM wave).

Columns: =Area= (hectares; GPS measure =s16aq47= where =s16aq45==1=,
else farmer estimate =s16aq09a= converted from its unit =s16aq09b=),
=AreaUnit= (always "hectares"), =Tenure= (=s16aq10=), =TenureSystem=
(=s16aq13=), =SoilType= (=s16aq18=), =Irrigated= (from water source
=s16aq17=).  Latitude / Longitude deferred (no decimal-degree parcel
GPS in EHCVM s16a).  The harmonize_* code maps live in
=categorical_mapping.org=.

Verification (2026-06-03): 9557 rows, unique =(t, i, plot_id)= index,
=is_this_feature_sane= passes, 0% orphan households vs =sample()=.
