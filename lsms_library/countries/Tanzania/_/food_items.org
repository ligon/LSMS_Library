#+title: Food Items (RETIRED -- migrated to harmonize_food)

# This standalone food-label table has been MIGRATED into the canonical
# shared label foundation (Unit #0).  The table now lives in
#   _/categorical_mapping.org   under   #+name: harmonize_food
# as a Uganda-style "| Code | Preferred Label | <per-wave columns> | FTC Label |"
# org table, so the (j) food axis can share Preferred Labels with the later
# crop_production / community_prices features (parity-loop GAP 1 / GAP C).
#
# See slurm_logs/2026-06-13_wb_incidence_map/PARITY_LOOP_DESIGN.md "Unit #0".
#
# Consumers updated: tanzania.harmonized_food_labels() now reads
# categorical_mapping.org:harmonize_food (default args).  No data_info.yml
# referenced this file (Tanzania food_acquired is the script path, which maps
# item labels at the country level via CONTENTS.org:food_labels).
