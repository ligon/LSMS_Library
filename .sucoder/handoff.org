#+title: Handoff -- 2026-09-12 (Sue / Claude Fable 5.1), Savio job 38714904

* State (2026-09-12, evening)
- *v0.12.0 RELEASED* (2026-09-12): PR #885 merged into master with a merge
  commit (2cbcbb046), tag =v0.12.0= on it (GitHub + mirror), GitHub
  release created from =docs/releases/v0.12.0.md=, #871-#878 auto-closed,
  =development= fast-forwarded to the same commit on both remotes.
  Verify PyPI shows 0.12.0 (=gh run list --workflow=publish.yml=); if the
  release event did not fire, =gh workflow run publish.yml --ref master
  -f tag=v0.12.0= (release skill).
- Merged today, each reviewed by a separate Opus agent: #878, #875, #877,
  #872, #873, #874, #876, #871 (most-complete observed row at all three
  collapses; reverses the 2026-07-13 completion doctrine), plus the
  derivations user guide + CLAUDE.md section, the #871 census
  (=bench/grain_census_871.py=), and the release notes.
- Filed, open: #879-#884.  Follow-ups from #871: #866 (rule-less var/
  targets do not rebuild on a hash move) was worked around by evicting
  the 18 affected parquets and rebuilding; the fix is still open.
- Caches: the WHOLE corpus was re-warmed after #871 (both tiers, from
  L1), 36 countries + the two space-named ones + the 18 #866 tables; 0
  build errors except Peru's known-absent cells.  Logs:
  =/local/job38714904/tmp/claude-43292/rewarm_all/= (node-local; gone
  with the job).
- Tests: full non-slow suite green at 86fd0af44 (5,871 passed) before
  #871; the post-#871 run on 0f4eff9a7 was in flight at handoff time --
  see the PR's status.  Recipe: CLAUDE.md addendum 3b.
- Coverage regrade done per country on the warm corpus (3304cf289): no
  tier moved.  Quirk: a single-country =make matrix= run does not apply
  the curated =blocked= verdicts (Nepal graded 'broken'); rows restored.

* Open questions for @ligon
- All merged refs deleted (mirror and local).  #879-#884 filed for the
  remaining findings; #871 parked.
- Coverage regrade is due for: Uganda crop_production (all 7 waves),
  Ethiopia food_acquired (all 5), the 12 EHCVM food_acquired cells,
  Niger people_last7days (both waves), Ethiopia shocks.  =make matrix=
  was not run today.
- Design follow-ons recorded in =SkunkWorks/derived_values.org=:
  =Feature()= re-attach of =attrs['derivations']= (with #873), derived
  table attrs, retrofit of the census sites, NSO request for GLSS
  visit dates (=slurm_logs/2026-09-11_ghanalss_visit_dates/FINDINGS.org=).
- ~350 in-tree workspace copies of DVC-tracked files in the main
  clone (not the worktrees) -- the library warns; cleanup command in
  =CLAUDE.md= ("A *.parquet inside countries_root() is never read");
  not run.

* Next concrete action
Wire the per-visit dates that 5 of 7 GLSS waves ship into
=interview_date= (=melt_visit_intervals=), per FINDINGS.org; or the
=Feature()= re-attach for #873.
