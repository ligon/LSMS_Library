#+title: Handoff -- 2026-09-17 (Sue / Claude Fable 5.1), Savio job 39002769

* GhanaAHIES: new country, in flight (2026-09-17)
Working clone =/local/job39002769/mirrors/LSMS_Library=, branch
=development=, last commit =99bfb8da8= (2024 pilot: sample,
household_roster, individual_education, food_security, housing).
Scaffold commit =0f7402895= (blobs on S3, provenance, catalog entry,
capability record, =_dvc_cmd= fix).  Findings:
=slurm_logs/ghana_ahies/README.org=; ledger =.coder/ledger/ghana-ahies.md=;
idiosyncrasies =lsms_library/countries/GhanaAHIES/_/CONTENTS.org=.

** Three worker agents were dispatched at ~14:20, each in its own worktree
   and branch (hook publishes the branch to the mirror):
- =/local/job39002769/worktrees/ghanaahies-2022= -> =feat/ghanaahies-2022=
  (the five tables for the 2022 folder)
- =/local/job39002769/worktrees/ghanaahies-2023= -> =feat/ghanaahies-2023=
  (same for 2023, plus the 2023Q3 half-scale hh_weight investigation)
- =/local/job39002769/worktrees/ghanaahies-2024-features= ->
  =feat/ghanaahies-2024-features= (cluster_features, people_last7days,
  anthropometry for 2024)
Private data roots under =/local/job39002769/data/= (gone with the job).
All three branches reviewed and merged (=a45fe175a=, =616742f0b=,
=181dfb210=; CONTENTS.org hand-merged twice); worktrees removed.  The
features merge's publish hit a hook race (two publishes overlapping left
the mirror's index dirty at =246341baf=): resolved by stashing the
mirror's staged copy, re-pushing, and dropping the stash -- avoid
committing again while a previous hook is still publishing.
Warm build done (shared cache, all twelve quarters, no grain/index/null
warnings); coverage graded (84 sane / 24 absent / 11 not-asked / 4
asked-not-distributed / 1 unsure / 1 undeclared).
Full suite run on the mirror at =26322d400= (Slurm 39008163, 16 cores,
58 min): 4 failed / 6,494 passed.  Two were GhanaAHIES's (no =Currency:=
entry; no =population.yml=) -- fixed and re-verified (139 passed in
test_population / test_currency / test_capability).  Two are GhanaLSS
=food_prices= row pins stale since =fe4f736f0= (#913's 8H valuation) --
pre-existing, filed as GH #943, not re-pinned.
Next actions: (1) re-run the full suite after #943 is resolved (recipe
below; the four other counts were green) (=_dvc_cmd= str->list at six call sites, capability record,
global =kinship.yml=, a new country every corpus-wide test now sees) --
burst it: =sbatch= on =co_carleton= / =savio4_htc= against the SHARED
mirror, =bin/savio_venv.sh mount= first (the mirror's =.venv= symlink is
dead), =.venv/bin/python -m pytest= (never =.venv/bin/pytest=: dead
shebangs), =-n $SLURM_CPUS_ON_NODE --dist=loadfile=, =LSMS_BUILD_WORKERS=1=
+ BLAS pins, log under =/global/scratch=; (2) clone =cluster_features=,
=people_last7days=, =anthropometry= into =2022/= and =2023/= (the 24
=absent= cells); (3) the 2023Q3 weight decision, the =config.yml=
restore, the =.dta= cleanup (all @ligon).

** Open for @ligon
- =~/.config/lsms_library/config.yml= was overwritten at 12:41 (only
  =statsghana_api_key= remains; =microdata_api_key=, =data_dir=,
  =phmecv_api_key= are gone).  Restore.
- Decide the 2023Q3 =hh_weight= factor of two (uniform x0.4986 on that
  column alone; see CONTENTS.org "The 2023 folder"): double in the wave
  script, or serve as shipped.
- Ask GSS for Sections 8-13 (consumption/expenditure/income/assets/NFE):
  in no distributed file; the questionnaire is the C4 evidence for
  =asked-not-distributed= verdicts on food_acquired etc. (add rows to
  =.coder/coverage/absent_verdicts.csv= once the country is in the matrix).
- The raw =.dta= copies still sit beside their =.dvc= sidecars in the
  clone's =GhanaAHIES/*/Data/= (7.6 GB, node-local, refused by the reader
  as "polluted"); blobs are on S3 and at
  =/global/scratch/fsa/fc_jevons/ligon/ahies_gss_2026-09-17/=.  Delete when
  convenient (=find lsms_library/countries/GhanaAHIES -name '*.dta' -execdir
  test -e '{}.dvc' \; -print -delete=).
- The squashfs venv image's console-script shebangs are dead
  (=#!/global/scratch/.../.venv/bin/python=); =_dvc_cmd= now sidesteps it,
  but =bin/savio_venv.sh update= should rewrite them.
- WB catalog check for an AHIES mirror (needs the WB key); =population.yml=
  entry for AHIES (edit the evidence base, re-promote).

* Earlier handoffs, GitHub-side stream (Handoff -- 2026-09-15 (Sue / Claude Opus 5), Savio job 38854624)


* v0.14.0 release session (2026-09-16 evening, Sue / Claude Fable 5.1, Savio job 38975827)

Written for @ligon to read on waking; every claim has a path or a PR number.

** What shipped

- Tag =v0.14.0= = =1528fa937= on =master= (release PR #938, merged with a merge
  commit); GitHub Release published, =publish.yml= fired on the release event.
  Notes: =docs/releases/v0.14.0.md=; closes list =docs/releases/v0.14.0-closes.txt=
  (#746 #763 #809 #866 #914 #919 #925 #929).
- PRs landed on =development= tonight, in order: #933, #924, #927, #930,
  #923, #934 (after fixing its CI failure: =PurchaseValue=/=Cost= missing
  from =currency._DEFAULT_MONETARY=, =0b8a1b532=), #935 (stacked on #934;
  four GhanaLSS pins moved at =8766f882c= for the label split), #940 (the
  re-curation of #937 on #935 -- the GhanaLSS =Aggregate Label= column).
- #937 is superseded by #940 and was closed by that merge's "Supersedes"
  reference or should be closed by hand if not.

** Decisions taken on your behalf (all reversible, all recorded)

- Two borderline Aggregate groupings, Goat/Mutton and Crab/Shrimp (gate
  ratio 1.21 primary vs 0.31 full wave), ship as SINGLETONS. Reasoning and
  numbers: =.coder/ledger/ghanalss-aggregate-recuration.md=. Regroup them in
  0.14.1 if you disagree.
- #937's Kapla/Tilapia group was dropped (fails the primary-wave gate, 1.17).
- The four =test_currency_denominated_units.py= GhanaLSS pins were MOVED for
  #935 rather than treating the failure as a defect: the crosswalk's split of
  seven collapsed labels returns rows every mode had merged; Expenditure per
  wave is unchanged. Commit message of =8766f882c= has the four deltas.
- =min_goods=1= scoring of the partition ranking is disclosed in the notes
  and left open as #939; no sweep was run.

** What CI cannot see (stated in the notes; worth knowing before class)

- The green =unit-tests= check skips ~1,550 data tests. The full suite was run
  locally on the release-candidate tree (development + #935): 6,484 passed,
  2 failed = the two pins above, then fixed. #940's data tests ran locally on
  the re-warmed cache: 452 passed, 4 skipped; pins held (3 passed).
- =SkunkWorks/aggregation/= tests never run in CI; 62 passed by hand with
  CFEDemands 0.10.0 shadowed on =PYTHONPATH=.

** Still open / not done

- =master='s =data-tests= job (the only CI job that runs data tests; it runs
  only on =master= pushes) is RED: 52 failures, all
  =tests/test_ehcvm_7day.py::TestDelivered= -- EHCVM =food_acquired= served
  without =Derivation= and without the inkind rows in the sequential cold
  session. It was also red on the v0.13.0 push. Reproduced NOT cold locally,
  NOT under CI's exact pandas/numpy/pyarrow, NOT with CI's parallel wave
  workers; module-name collision and cache-tier ordering also eliminated.
  The served CI frame is the raw YAML extraction -- the wave =df_edit= hook
  did not run. My first hypothesis (=derivations.yml= outside the hash) is
  refuted (the key is a literal; the hook stamps unconditionally) and is
  amended in a comment on #941 with the eliminated list and what survives. No
  evidence pip users are affected; not a hotfix unless the bisection says so.

- The main checkout =/global/scratch/fsa/fc_jevons/ligon/mirrors/LSMS_Library=
  is on =development= at =6666e0fea=, now far behind origin. I did not
  fast-forward it (other jobs import from it). =git pull --ff-only= there is
  clean.
- The shared =.venv= has CFEDemands 0.8.2; the library does not import it, but
  the aggregation pipeline needs >=0.10.0 (=pip install -U CFEDemands= into
  the venv image via =bin/savio_venv.sh update= when convenient).
- #939 (min_goods sensitivity), #831 (two remaining bullets), #894 item 2
  (Malawi/Nigeria panel_ids to design A) stay open.
- Worktrees left in place: =worktrees/panel-estimation= (branch merged),
  =worktrees/stack-935= (branch =docs/v0.14.0-notes-940=, merged). Both are
  safe to =git worktree remove=.

** Verify from a fresh venv

#+begin_src sh
pip install -U LSMS_Library==0.14.0
python -c "import lsms_library as ll; print(ll.__version__)"
python -c "import lsms_library as ll; print(ll.Country('GhanaLSS').food_expenditures(labels='Aggregate').shape)"
#+end_src


* GhanaLSS food-label crosswalk session (2026-09-16, PR #935)

Merged from =fix/ghanalss-label-crosswalk='s own handoff note when the branch was
brought up to date with =development= (2026-09-16); headings demoted one level,
text otherwise verbatim.

** State

- Branch =fix/ghanalss-label-crosswalk=, *5 commits*, 5 ahead of
  =development=, 0 behind.  Working tree clean.  Published to the shared
  mirror and (as of this note) pushed to GitHub with a PR open.
- Last commit: =684120114= "reconcile guinea corn + sorghum at Lcp with a
  named reducer".
- Full record: =slurm_logs/ghanalss_aggregate_labels/= -- =FINDINGS.org= (the
  ladder + the original diagnosis), =TABLE_AUDIT.org= (every defect found),
  =LANDED.org= (what shipped, with the decisions and who made them), plus 12
  re-runnable scripts and =README.md= saying how to run them.

** What the task was, and what it turned into

Asked: find =Aggregate= labels for GhanaLSS =food_expenditures=.
Found: =labels='Aggregate'= raises, and the two places that record this as
deliberate name three blockers of which *two were stale*.  The one that binds
was unrecorded: the country =harmonize_food= table carries a per-wave
crosswalk (=Lc . Lcp= in @ligon's ladder) that *was never applied*, so the
served =j= was the WAVE vocabulary and 45 of 225 labels sat off the country
axis -- 14% of rows, ~36% of GLSS2 expenditure, silently.

So steps 1-4 fixed the =j= axis.  *Step 5 -- designing the =Aggregate Label=
column -- is NOT started*, by agreement: @ligon wanted to discuss principles
of aggregation first.  That is the next conversation.

** Invariants now holding at HEAD

| check                                | value                 |
|--------------------------------------+-----------------------|
| served =j= off the country axis      | *0* (was 45 of 225)   |
| duplicate index tuples               | *0*                   |
| distinct =j= vs country labels       | *215 = 215* bijection |
| Expenditure per wave, before/after   | identical, all seven  |
| =test_ghanalss_food_label_canonical= | *32 passed*           |

Two new guards: =TestDelivered= now asserts =j= is on the COUNTRY axis (it
asserted the WAVE axis, which was right for =Lw= and could never have caught
this), and a config-only per-wave bijection test.

** Next steps, in order

1. *Design =Aggregate Label=* (step 5).  Read =FINDINGS.org= "The framing
   number" first: a mechanical reconciliation of the wave columns yields
   195 -> 134 buckets with *91 singletons*, against Malawi 147 -> 65 and Uganda
   175 -> 80.  The wave-level Aggregate columns were a spelling layer, not a
   demand-estimation bucketing.  GH *#226* is the principled auto-aggregation
   script this probably belongs in; GH *#925* constrains it -- a blank cell
   becomes one unnamed bucket, so the column must be complete for all 215
   labels.
2. *Rebuild and inspect =nutrition=.*  NOT done.  It will change: the
   Cocoyam/Coffee fix (8,935 rows), gari 02_004 -> 02_039, sweet potato
   02_022, and the deliberately-blank =Oil (palm)/Shea Butter= composite
   (4,239 rows drop out).  Do this before anyone quotes a GhanaLSS nutrient
   number.
3. *The latent =.first()=* at =1998-99/_/food_acquired.py:110= -- filed as its
   own issue; does not fire today.

** Traps worth knowing (all cost time this session)

- =.pth= points at =/global/scratch/.../mirrors/LSMS_Library=, a THIRD
  checkout.  Always run wave scripts with both
  =PYTHONPATH=<clone>= and =LSMS_COUNTRIES_ROOT=<clone>/lsms_library/countries=,
  and assert =/local/job*/ in lsms_library.__file__=.
- =2005-06/_/categorical_mapping.org= has *mixed CRLF/LF* line endings.  A
  naive read/write normalises the whole file and buries the real edit in a
  372-line diff.  Preserve per-line terminators.
- Org tables *right-align numeric columns*.  Regenerating a wave table with a
  left-justifying writer rewrites every row.  Edit the changed LINES in place.
- =groupby().agg('sum')= returns *0.0 for an all-NA group*.  Use
  =min_count=1=, or phantom rows sail past a later =dropna(how='all')=
  (+32,040 of them here).
- Do not compute an "expected" row count from an assumed baseline.  Rebuild
  the true prior state and measure; that is what finally resolved a +637 that
  three rounds of reasoning had not.
- The shared mirror's =.venv= symlink points at whichever job last ran
  =bin/savio_venv.sh mount=.  Two agents on different nodes will ping-pong it.
  A clone's own =.venv= should point straight at its mount.

* Mirror cleanup and the #930 rebase (2026-09-15 afternoon, job 38854624)

Supersedes the next-action suggestions below.  Worked *directly in the shared
mirror* (=~/mirrors/LSMS_Library=, which resolves to
=/global/scratch/fsa/fc_jevons/ligon/mirrors/LSMS_Library=) on =development=,
at @ligon's direction -- not in a node-local clone.  Note that the WIP-snapshot
safety net watches the clone, not the mirror, so work here is durable only once
committed.

** Two corrections to the 08:49 section below, which is left verbatim

1. *=development= was never red.*  That section says "development red until
   #930"; #930's own commit message said the Ethiopia tests "have been failing
   on development since #898".  Both were measured against the shared mirror's
   =development=, which was *15 commits behind GitHub*.  #919 (=7bb2d9955=,
   merged 2026-09-14) had already fixed the same root cause by the opposite
   route: it REMOVED the =quintal=100= seed from =KNOWN_METRIC= instead of
   working around it.  Verified against GitHub's =development= (=6666e0fea=):
   both named tests PASS, and 93 passed across the three files #930 touches.
   =git merge-base --is-ancestor 7bb2d9955 3d5915cb8= is *false* -- that is the
   one-line proof #930 was built without #919.
2. *#930's head had already moved* past the =3d5915cb8= recorded below
   (=8d9df49d7=, 12:54 that day), and has since been rebased to =ffcae2797=.

*The general lesson, which cost this session an hour to unpick*: a claim about
=development= measured in the mirror is not a claim about =development=.  Check
=git rev-list --left-right --count development...origin/development= after a
=fetch= before believing one.

** What landed

- *Mirror =development= fast-forwarded* =7b7d330fd= -> =6666e0fea=, clean
  (0 divergent).  It had been 15 behind; its =origin/development= tracking ref
  was staler still (=3b6a0fda9=), which is why it read "ahead 25".
- *PR #930 rebased* onto =6666e0fea= and force-pushed (=--force-with-lease=
  pinned to =8d9df49d7=) -> =ffcae2797=.  Now MERGEABLE.  Retitled and its body
  rewritten; the old title claimed it restored an external check that #919 had
  already restored.  *Layer split only*: the =test_quintal_...= re-expression
  and the pinned-totals swap were DROPPED, because taking them would have
  downgraded the check #919 repaired.
- *Filed [[https://github.com/ligon/LSMS_Library/issues/931][#931]]* -- =harvest_kg_factors= never receives #919's =unit_kg=, so the
  crop ladder is blind to declared =u_kg= rows.  That blindness is LOAD-BEARING
  (it is what keeps Ethiopia's external check non-tautological) and is
  undocumented; fixing it silently would re-break the check.
- *Filed [[https://github.com/ligon/LSMS_Library/issues/932][#932]]* -- 247 =requires_s3= tests of 6,482 skip on every PR and on
  =development=, so a red suite reports green.  This is thread 1 of the 08:49
  section, now filed.  It is *why* the stale-base claim above travelled through
  a commit message, a PR body and a handoff note unchallenged.  Confirmed live:
  #930's own check run shows =data-tests: skipping=.
- *Mirror worktree/branch cleanup*: 95 worktrees -> 8, 132 branches -> 39.

** Measured, so it need not be re-derived

#930 re-measured on the correct base (=measure_930.py= in the record dir):

| quantity | value |
|----------+-------|
| =kg_per_unit= differing, either split | *0 of 85,519*, NaN pattern identical |
| =Harvest_kg= total | 12,680,617.283538, identical both trees |
| before | =inferred= 26,548 -> =metric= 26,548; =none= 58,971 unchanged |
| after | =inferred= 7,768 -> =metric= 7,768; =none= 15,587 unchanged |
| ratio =at/bt= | 6.654, so the original =assert > 6 *= holds |
| verification | 148 passed, 0 failed, across six test files |

=inferred= is *0 in both splits* -- the price-ratio inference serves Ethiopia
not one crop row.  That corpus fact survives #919 and is now pinned.

Every number in #930's ORIGINAL message (=at.sum()= 12,691,567, =bt.sum()=
8,799,669, ratio 1.44) was an artifact of the missing base.  Do not quote them.

** Mirror hygiene

- Kept deliberately: =master=, =development=, the 4 open-PR branches, 15
  mirror-only unmerged branches (incl. 8 =fix/prices-*=), 4 where the mirror is
  AHEAD of GitHub (=chore/feature-vocabulary-nutrition-nonfood= by 25 commits),
  and 7 worktrees with uncommitted tracked edits plus the 7 branches they pin.
- *Those 7 dirty worktrees are the remaining tidy-up*: resolve the edits, then
  their branches are deletable.  =wt-801-mojibake= has 13 modified files.
- 9 untracked =slurm_logs/= files were tarballed before their worktrees were
  removed (4 Lorenz PNGs, =REVIEW_784.org=, 3 GLSS probe logs).
- 50 untracked files remain under =slurm_logs/= in the mirror's own tree --
  briefs, matrix runs, 6 =HANDOFF_*.org=.  Either commit or clear them; today
  they are neither.

** Record

Everything above is reproducible from
=/global/scratch/fsa/fc_jevons/ligon/tmp/wt-cleanup-2026-09-15/= (durable
scratch, NOT node-local): =CLEANUP_RECORD.org=,
=FINDING_930_superseded.org=, =measure_930.py= with its JSON/npy outputs, and
the removal/deletion logs.

** Next concrete action

Merge #930 once CI is green (=unit-tests= was in flight at handoff).  Then
either #932 (the CI gap -- it is the one that lets this class of error repeat)
or #928's P1/P2, which still needs its cache-invalidation measurement first.

* Earlier handoff: Handoff -- 2026-09-15 (Sue / Claude Opus 5), Savio job 38854624

* Categorical-mapping session (2026-09-14/15, job 38854624)

Supersedes the next-action suggestions below.  Working clone
=/local/job38854624/mirrors/LSMS_Library=; =origin= is the shared mirror.
Nothing was merged.  Two PRs are open for human review.

** Open for merge

| PR | issue | branch | head | re-warm cost |
|----+-------+--------+------+--------------|
| [[https://github.com/ligon/LSMS_Library/pull/927][#927]] | #925 | =fix/relabel-j-empty-string= | =156279ef8= | *GhanaLSS, 17 of 119 table hashes* |
| [[https://github.com/ligon/LSMS_Library/pull/930][#930]] | #929 | =fix/838-ethiopia-pinned-numbers= | =3d5915cb8= | none (0 of 119) |

*#930 un-reds =development=.*  Two assertions in
=tests/test_ethiopia_shipped_factors.py= have been failing since #898 merged.

** Issues filed

- #925 :: =labels=<col>= renamed an unlabelled food to the empty string and
  =reaggregate=True= summed 129 served foods into one unnamed =j= bucket
  (GhanaLSS =1987-88=, 55,314 rows, Expenditure conserved).  Fixed by #927.
- #926 :: GhanaLSS =nonfood_expenditures= serves 25,585 rows with cp1252 C1
  controls in =j=.  The bad code points are committed in curated config
  (=ebd38d393=), so the #805 reader fix will NOT clear them.  *Unfixed.*
- #928 :: proposal, *awaiting review* -- name the categorical key column
  (=#+key:= / =idxvars=) and the ignorance vocabulary (=Status= / =Notes=,
  =FIXME= / =UNDECODABLE=).  Refers to #813.  Survey committed at
  =slurm_logs/categorical_key_vocabulary/= (config-only, seconds to run).
- #929 :: crop kg ladder had no =metric= layer.  Fixed by #930.

** What #930 actually fixes, because the framing matters

#838 is CORRECT (a quintal is the metric centner; 13,978 Ethiopian rows that
converted to nothing now convert).  What it also did was turn the WB shipped
table's only EXTERNAL check into a tautology -- the same failure the test's own
docstring disowns for =Kg=/=Gram=.  Re-baselining =13,924 -> 0= would have
passed CI and retired the check silently.  Instead the crop ladder gained the
=metric= layer the food ladder has had since #850.  Relabelling only: 0 of
85,519 =kg_per_unit= values differ, Harvest_kg totals identical, 0 cache
hashes moved.

Fell out of it: *=inferred= is now 0 for BOTH Ethiopia and Uganda* -- the
crop-side price-ratio inference serves neither country a single row.

** Open threads, in the order I would take them

1. *CI gating -- not filed, and it is why development can sit red.*  The
   credentialed =data-tests= job runs only on pushes to =master= /
   =separate_data=, so all *247 =requires_s3= tests of 6,482* skip on every PR
   and on =development=.  #930 fixes the current redness; the gap remains.
   Options discussed, none chosen: run data-tests on PRs to development; run
   only the pinned-number subset; a scheduled nightly; or decide it is
   deliberate.
2. *#928 review.*  P1/P2 (=#+key:=, consolidating the four positional
   key-column sites) are library-side and free -- but MEASURE first:
   =all_dfs_from_orgfile= is reached from the =@build_transform=-tagged
   =df_data_grabber=, so it may invalidate corpus-wide.  P3/P4 are per-country
   and must be batched (any =categorical_mapping.org= edit costs that country
   a re-warm).
3. *Malawi's four kilograms* (#832).  Malawi serves =Kg= / =kg= / =Kilogram= /
   =Kilogramme= over 146,284 rows while Uganda, Nigeria, Niger, Benin and Togo
   serve one.  =categorical_mapping/u.org= holds the unifying rows and cannot
   reach Malawi: =_row_union_categorical= falls back silently when key columns
   disagree (=u= aligns for 9 countries, falls back for 4).
4. *"Site M"* -- discussed, not decided.  #693, #694, #674, #925 and the merge
   fallback are one failure class: the loader returns something that LOOKS
   like a valid answer.  Every other layer in this library has a named audit
   site; the categorical-mapping layer has none.  The open question is the
   TRIGGER, not the site -- per the null-read guard's own history, a per-column
   form fired 887 times and the frame-fraction form 0.
5. *#926* -- the C1 controls need a config repair, independent of #805.

** Measured facts worth not re-deriving

- =---= parses to NaN, a blank cell to the empty string.  So *NaN means
  somebody wrote the sentinel; empty string means somebody left it blank.*
- =Country.data_scheme= is a LIST of table names, not a dict; and
  =data_scheme.yml= nests tables under =Data Scheme:= behind =!make= tags.
  Both produced confident wrong answers during this session.
- =Country._table_cache_hash= takes =(table, waves)=.  Calling it with one
  argument and catching the TypeError compares error strings, not hashes --
  that is how "0 of 12 moved" got into a commit message wrongly.
- =CONTENTS.org= is the one =.org= the cache hasher skips
  (=_ORG_HASH_SKIP=), so prose there is free.
- Panama's quintal: =units.json= says 220.462 lb = 100.000 kg; =units.py=
  overrides to 100 lb and wins.  @ligon adjudicated 2026-09-15 that the
  override is right and the source compiler mistook a Spanish measure for a
  universal one.  Recorded in =Panama/_/CONTENTS.org=.

** Session hygiene note

Three separate measurements in this session were wrong on first run (a broken
cache probe, a cross-branch hash comparison, and two bad =data_scheme= reads).
Each was caught before or shortly after reporting, and =45fb9fa8c= exists
solely to correct five numbers and one mechanism an independent red-team found
in my own earlier commits.  Treat any figure here without a committed
reproducer as needing one.

* Next concrete action

Merge #930 to un-red =development=, then decide #928's P1/P2 or file the CI
gating gap.

* Earlier handoff: Handoff -- 2026-09-12 (Sue / Claude Fable 5.1), Savio job 38714904

* GhanaLSS backlog session (2026-09-12, Sue / GPT-6, job 38734650)

This section supersedes the older next-action suggestions below. The working
clone is =/local/job38734650/mirrors/LSMS_Library=, branch =development=;
=origin= is the durable shared mirror. No PR was auto-merged.

** Changes prepared for human merge

| issue | PR | branch | code commit | result |
|-------+----+--------+-------------+--------|
| #753 | [[https://github.com/ligon/LSMS_Library/pull/886][#886]] | fix/753-ghanalss-config-root | 19b94acca | Four early-wave label maps honor countries_root |
| #826 | [[https://github.com/ligon/LSMS_Library/pull/887][#887]] | docs/826-ghanalss-price-quantity | 4bb08cf6c | Qualify reported QTY; preserve raw fields |
| #757 | [[https://github.com/ligon/LSMS_Library/pull/888][#888]] | fix/757-country-wave-inputs | 27ab8b285 | Inherited country/global Org edits invalidate wave caches |
| #780 | [[https://github.com/ligon/LSMS_Library/pull/889][#889]] | fix/780-ghanalss-fingerprint | ee8a63e05 | Fingerprint compiled patterns; warn on degraded fingerprints |

All branch commits were published to origin by the post-commit hook and
pushed to GitHub. Each fix has isolated worktree verification and independent
review. Per-task ledgers for #757/#780 were committed separately.

The user explicitly chose documentation-only handling for #826. The 5,351
screened-out GLSS7 observations are not all confirmed gram entries. Millet
quantity is 4000 at cluster 70735 and 4100 at 70789/70791; the raw-cache
=Kilogram= label must count as the API's =Kg= when reproducing the screen.
No automatic unit correction, filtering, or imputation was introduced.

** Verification and cache implications

- #753: eight new sentinel cases fail on original code; override suite 12
  passed plus one existing rural regression. Scoped Feature audit retained
  identical roster/sample/cluster cached DataFrames and substantive findings.
- #757: 22 tests passed in worker and independent review. Restoring the old
  hash method makes the synthetic Old -> mapping edit -> stale -> New cache
  regression fail. Country-only edits remain country-scoped; global edits
  invalidate broadly. Initial sampled hashes changed 46/46 by design.
- #780: 35 tests passed, including all country import closures and Ghana
  cross-process determinism. Standalone pattern coverage also picks up the
  shared DCT parser's previously omitted pattern: all five control hashes
  change; removing only standalone recognition restores their old hashes.
  Normal warnings retain best-effort loading. Under caller-selected
  warnings-as-errors the wave warning raises and the country outer catch
  returns None; this is explicitly tested.
- Combined targeted suite: 69 passed, 16 existing warnings, using
  =-n 32 --dist=loadfile=, =LSMS_BUILD_WORKERS=1= and =--no-purge=.
- Both cache fixes deliberately cause initial broad invalidation. No shared
  cache was purged; data verification uses private L2 copies and shared L1.
  Even #826 documentation edits can move Ghana hashes because source files
  and data_scheme.yml are hashed whole.
- Combined GhanaLSS/Albania Feature audit: all eight default API frames
  identical before/after under symmetric parquet roundtrip, including values,
  labels, indexes and dtypes. All 34 baseline substantive findings remain.
  Ten additional rebuild warnings concern Albania's Makefile, absent in both
  checkouts; no new schema/assembly/data findings. Source stderr about 711
  null-PSU person rows and 83 sentinel-coded households matches Albania's
  CONTENTS.org. Existing unclosed-file ResourceWarnings decreased 522 -> 48.
  GitHub CI was still pending at handoff.

Reproduce targeted tests with the checkout as cwd, =PYTHONPATH= pointing to
it, and the main clone's =.venv/bin/python -m pytest= on
=tests/test_countries_root_override.py=,
=tests/test_cache_hash_invalidation.py=, and
=tests/test_build_transform_hash.py=. Feature audit scope:
=--features household_roster sample cluster_features individual_education
--countries GhanaLSS Albania --phases 1=. GitNexus tools were unavailable;
review used ripgrep call-site analysis, diffs, and executable regressions.

** Reconciliation and remaining decisions

- Closed #563: nutrition already implemented in PR #769; 10 current tests pass.
- Closed #770: currency-as-mass defect already fixed in PR #784; 16 current
  non-slow regression tests pass.
- #797 Ghana relationship harmonization already implemented; global kinship
  variant cleanup remains human-owned. #873 derivation attrs also already
  fixed, despite the older handoff suggestion below.
- #783 vocabulary migration landed in #789, but Ghana's Aggregate column
  remains intentionally absent pending the documented CFE curation decision.
- #832 Ghana unit spellings already fixed; other countries remain in scope.
- #709 pandas >=3.0 floor already landed; ancillary requests remain.
- #562 phase 3a community prices exists for six waves. Phase 3b inverse
  imputation remains a design task; GLSS5 prices are asked-not-distributed.
- #826 remains open for consumer quantity-basis handling. Visit-date wiring,
  recall normalization (#851), and broad cache issues #778/#809 remain separate.

Next action: review/merge the four PRs after CI, then choose the next Ghana
data-design task. Keep the source-field preservation decision above intact.
The node has 32 CPUs / 93 GiB RAM; FCA usage at setup was 42,593.38 of
1,500,000 SUs. The prepared venv image is mounted node-locally; the empty
bootstrap venv was preserved as =.venv.bootstrap=. Release the billed node
with =sucoder release= when finished.

* State (2026-09-12, evening)
- *v0.12.0 RELEASED* (2026-09-12): PR #885 merged into master with a merge
  commit (2cbcbb046), tag =v0.12.0= on it (GitHub + mirror), GitHub
  release created from =docs/releases/v0.12.0.md=, #871-#878 auto-closed,
  =development= fast-forwarded to the same commit on both remotes.
  Verify PyPI shows 0.12.0 (=gh run list --workflow=publish.yml=); if the
  release event did not fire, =gh workflow run publish.yml --ref master
  -f tag=v0.12.0= (release skill).
- Merged today, each reviewed by a separate Opus agent: #878, #875, #877,
  #872, #873, #874, #876, #871 (most-complete observed row at all three
  collapses; reverses the 2026-07-13 completion doctrine), plus the
  derivations user guide + CLAUDE.md section, the #871 census
  (=bench/grain_census_871.py=), and the release notes.
- Filed, open: #879-#884.  Follow-ups from #871: #866 (rule-less var/
  targets do not rebuild on a hash move) was worked around by evicting
  the 18 affected parquets and rebuilding; the fix is still open.
- Caches: the WHOLE corpus was re-warmed after #871 (both tiers, from
  L1), 36 countries + the two space-named ones + the 18 #866 tables; 0
  build errors except Peru's known-absent cells.  Logs:
  =/local/job38714904/tmp/claude-43292/rewarm_all/= (node-local; gone
  with the job).
- Tests: full non-slow suite green at 86fd0af44 (5,871 passed) before
  #871; the post-#871 run on 0f4eff9a7 was in flight at handoff time --
  see the PR's status.  Recipe: CLAUDE.md addendum 3b.
- Coverage regrade done per country on the warm corpus (3304cf289): no
  tier moved.  Quirk: a single-country =make matrix= run does not apply
  the curated =blocked= verdicts (Nepal graded 'broken'); rows restored.

* Open questions for @ligon
- All merged refs deleted (mirror and local).  #879-#884 filed for the
  remaining findings; #871 parked.
- Coverage regrade is due for: Uganda crop_production (all 7 waves),
  Ethiopia food_acquired (all 5), the 12 EHCVM food_acquired cells,
  Niger people_last7days (both waves), Ethiopia shocks.  =make matrix=
  was not run today.
- Design follow-ons recorded in =SkunkWorks/derived_values.org=:
  =Feature()= re-attach of =attrs['derivations']= (with #873), derived
  table attrs, retrofit of the census sites, NSO request for GLSS
  visit dates (=slurm_logs/2026-09-11_ghanalss_visit_dates/FINDINGS.org=).
- ~350 in-tree workspace copies of DVC-tracked files in the main
  clone (not the worktrees) -- the library warns; cleanup command in
  =CLAUDE.md= ("A *.parquet inside countries_root() is never read");
  not run.

* Next concrete action
Wire the per-visit dates that 5 of 7 GLSS waves ship into
=interview_date= (=melt_visit_intervals=), per FINDINGS.org; or the
=Feature()= re-attach for #873.
