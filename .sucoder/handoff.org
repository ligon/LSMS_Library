#+title: Handoff -- 2026-09-11 (Sue / Claude Fable 5.1), Savio job 38714904

* State
- Branch =development=, mirror published through 568993554.  Merges today:
  8499ffed8 (feat/derived-values: the derived-values mechanism + GhanaLSS
  12B served as a labelled derived fortnight) and c4cb87162
  (fix/recall-record-dict: =RecallRecord= is a dict subclass; the 17
  housing =to_parquet= failures are green).  Earlier today: ddfe2726c
  (GLSS5 s8hq3 drop fixed), 6d891861c (GLSS visit-date audits).
- Tests: not-slow suite on the merged tree 5,666 passed / 17 failed
  BEFORE the recall merge; those 17 are green after it (159 targeted
  tests pass on 568993554).  Run the suite as =CLAUDE.md= addendum 3b
  says: =-n N --dist=loadfile= with =LSMS_BUILD_WORKERS=1=.
- Shared cache: all 11 GhanaLSS L2 tables re-warmed at the merged hash
  (see the re-warm logs in the session scratchpad; =nutrition= was last).
- Issues filed today: #871-#878 (see =SkunkWorks/derived_values.org=
  "Filed / adjudicated 2026-09-11" for what was filed, what was not,
  and why).

* Open questions for @ligon
- Two dead refs on the mirror to delete: =feat/derived-values=
  (e4c521281, pre-rebase history) and =feat/derived-values-rebased=
  (56188dd26, merged).  Also =fix/recall-record-dict= (48c97caaf,
  merged).  Never force-pushed; deletion is yours.
- Framework-grain defect not yet filed: =data_info.yml:313-318=
  declares plot =Area= "in hectares" while 11 of 22 countries serve
  another =AreaUnit= (Tajikistan sotka, Albania sq m, ...).  File?
- GhanaSPS =MonthsAway= NaN->0: disclosed decision, but
  =GhanaSPS/_/CONTENTS.org:594-650= still says wave 1 is "not wired";
  and Nigeria W4's last-purchase pairing (=Nigeria/_/CONTENTS.org:50-63=)
  says "worth revisiting as its own issue".  Neither filed.
- Design follow-ons recorded in =SkunkWorks/derived_values.org=:
  =Feature()= re-attach of =attrs['derivations']= (with #873), derived
  table attrs, retrofit of the census sites, NSO request for GLSS
  visit dates (=slurm_logs/2026-09-11_ghanalss_visit_dates/FINDINGS.org=).
- ~350 in-tree workspace copies of DVC-tracked files in the main
  clone (not the worktrees) -- the library warns; cleanup command in
  =CLAUDE.md= ("A *.parquet inside countries_root() is never read");
  not run.

* Next concrete action
Wire the per-visit dates that 5 of 7 GLSS waves ship into
=interview_date= (=melt_visit_intervals=), per FINDINGS.org; or the
=Feature()= re-attach for #873.
