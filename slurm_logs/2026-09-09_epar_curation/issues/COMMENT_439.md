EPAR's Uganda, Tanzania and Ethiopia do-files build almost exactly the grain this issue asks for, then collapse it away -- and the raw columns are in files our own wave scripts already open. This is input to the schema proposal filed as #862. EPAR paths below are relative to `reference/epar/LSMS-Agricultural-Indicators-Code/` at HEAD `abfbdc9301242527b7543606aad1ad6eed2e6530`.

**The construction.** `EPAR_UW_Uganda_UNPS_W5.do:413-424` reshapes `a3aq3_3` / `a3aq3_4a` / `a3aq3_4b` (with their B-season twins) long over `(hhid, parcel_id, plot_id, season)` and saves `Uganda_NPS_W5_dm_ids.dta`. Its columns are those six plus `formal_land_rights` from the parcel-rights collapse at `:397-401` and `personid, female, age, hh_head` from `Uganda_NPS_W5_person_ids.dta` (`:278`). The next statement, `collapse (mean) female (firstnm) dm1_gender, by(hhid parcel_id plot_id season)` (`:433`), reduces it to one plot-season gender categorical. Tanzania saves the same shape (`EPAR_UW_Tanzania_NPS_W3.do:261`, collapsed at `:263` by `(hhid, plot_id, season, cultivated)`), as does Ethiopia (`EPAR_UW_Ethiopia_ESS_W4.do:515`, no role slot).

**All 21 raw ids are unwired, and the files carrying them are already open.** `a3aq3_3` / `a3aq3_4a` / `a3aq3_4b` plus B-season twins (6), Tanzania's `ag3a_08_1/_2/_3` and `ag3b_08_1/_2/_3` (6, `W3.do:248-253`) plus the `ag3a_08b_*` / `ag3b_08b_*` NPS-W5 spelling (6, `W5.do:298-303`), and Ethiopia's `s3q13` / `s3q15_1` / `s3q15_2` (3) return zero hits across `lsms_library/`. Uganda 2015-16's `plot_labor.py:25-26` and `plot_inputs.py:22-23` already open `AGSEC3A.dta` / `AGSEC3B.dta`; Ethiopia 2018-19's `plot_inputs.py:28`, `plot_labor.py:21` and `plot_features.py:20` already open `sect3_pp_w4.dta`.

**Tanzania is the exception.** Our 2019-20 and 2020-21 waves hold `AG_SEC_3A.dta` / `ag_sec_3a.dta`, so if the `ag3a_08b_*` columns are present in our copies (not checked here) those two are column-adds. `Tanzania/2008-15/Data/` -- which is where EPAR's W3 (2012-13) sits in our multi-round folder -- holds no `ag_sec_*` file at all, so that wave is a source acquisition, not a wire-up.

**The grain in the design doc is missing a level.** `(t, i, plot, pid, role)` drops `season`, but Uganda collapses by `(hhid, parcel_id, plot_id, season)` and Tanzania by `(hhid, plot_id, season, cultivated)`, and `crop_production` already carries `season` (`data_info.yml:578`, `(t, i, plot, j, u, condition, season)`). Without it, two seasons on the same plot land on `_normalize_dataframe_index`'s `groupby().first()` -- and CLAUDE.md's rule is that duplicates on a declared index mean a level is missing, not that a reducer is wanted.

**The repo has already reserved this table.** `data_info.yml:228-232`, in the `plot_features` block: "Per-season detail (which crop, who farmed it this season, season-specific inputs, crop-share %) belongs in a future `plot_cultivation` table". `slurm_logs/DESIGN_erhs_plot_features_2026-05-20.org:54-57` says to file that as a new issue rather than retrofit `plot_features`. "Who farmed it this season" is EPAR's construct exactly, so the proposal belongs under that name and grain, not as a standalone `plot_decision_makers`. Ethiopia's `holder_id` is not a missing level for it: `ethiopia.py:748` and `:1000` already fold `holder_id` into `plot_id`.

**`individual_assets` is two collapses from EPAR's intermediate, not one.** In `EPAR_UW_Ethiopia_ESS_W4.do`, the productive-asset domain reads `sect11_hh_W4.dta` (`:3553`), filters on `asset_cd` (`:3555`), then drops `asset_cd` at `:3557` (`keep hhid` plus the twelve owner slots `s11q02_1..12`, `:3556`) before the per-slot loop (`:3561`) reshapes to a 3-value `type_asset` tag. The land and livestock domains use the same loop shape (`:3507`, `:3534`) on files that have no item code to begin with. The better path skips the intermediate: `sect11_hh_w4.dta` is already `(hhid, asset_cd)` with twelve owner slots per row, so a reshape long that keeps `asset_cd` gives `(i, j, pid)` directly, at finer item resolution than anything EPAR built. Uganda's women's-asset construct has the identical structure (`EPAR_UW_Uganda_UNPS_W5.do:3223`, `keep hhid type_asset asset_owner*`). Tanzania W3 carries all four women's-module banners (`W3.do:2930`, `:3049`, `:3241`, `:3387`), including its own rights question (`ag3a_31_1/_2`, `:3410-3416`).

Source: slurm_logs/2026-09-09_epar_curation/LEARNINGS.org
